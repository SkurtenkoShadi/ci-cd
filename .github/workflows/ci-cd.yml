name: CI/CD Pipeline for Python

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # Обновлено до v4 для совместимости

    - name: Set up Python 3.x
      uses: actions/setup-python@v5  # Обновлено до v5 (последняя стабильная)
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
        pip install -r requirements.txt

    - name: Lint with flake8
      run: flake8 hello_script.py --count --select=E9,F63,F7,F82 --show-source --statistics

    - name: Build package
      run: python setup.py build

    - name: Deploy artifact to GitHub
      uses: actions/upload-artifact@v4  # Обновлено до v4
      with:
        name: built-package
        path: build/  # Сохраняет папку build с собранным пакетом (теперь поддерживает glob, например build/**/*)
        retention-days: 7  # Опционально: хранить артефакт 7 дней (для лабораторной)
